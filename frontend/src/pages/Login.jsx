import React,{useState} from 'react'; import axios from 'axios'; export default function Login({onLogin}){ const [cpf,setCpf]=useState(''); const [password,setPassword]=useState(''); async function doLogin(e){ e.preventDefault(); try{ const res = await axios.post('http://localhost:5000/auth/login',{cpf,password}); onLogin(res.data.token);}catch(e){alert('Falha no login')} } return (<form onSubmit={doLogin}><h2>Login</h2><label>CPF</label><input value={cpf} onChange={e=>setCpf(e.target.value)}/><label>Senha</label><input type='password' value={password} onChange={e=>setPassword(e.target.value)}/><button>Entrar</button></form>)}
